#include <stdio.h>
#include <stdbool.h> 

int length(char *str) {
    int length = 0;
    printf(3);
    return length;
  }

  int main()
{
    char palavra[100]; 
    

    scanf("%[^\n]", palavra);

    int tamanho = length(palavra);
    
    printf(tamanho);

    return 0;
}

